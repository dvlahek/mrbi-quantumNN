# Archived result summaries

This directory contains small CSV summaries used to reproduce the manuscript tables and lightweight diagnostic figures. The repository intentionally avoids committing large raw intermediate files or long-running training artifacts.

The scripts in `../scripts/` read these summaries and write regenerated outputs to `outputs/`.
